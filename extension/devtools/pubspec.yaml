name: interceptify_devtools
description: "DevTools extension for Interceptify network interceptor."
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  devtools_extensions: ^0.5.1
  devtools_app_shared: ^0.5.1
  devtools_shared: ^13.0.1
  vm_service: ^14.1.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
